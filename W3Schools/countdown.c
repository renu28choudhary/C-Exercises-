// Use a while loop to create a simple "countdown" program:

#include <stdio.h>

int main()
{
    int count = 10;

    while (count > 0)
    {
        printf("%d \n", count);
        count--;
    }
    printf("happy New year !!");
    return 0;
}