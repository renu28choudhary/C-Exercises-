// Calculate the area of a rectangle (by multiplying the length and width)

#include <stdio.h>

int main()
{
    float length = 4.5;
    float width = 8.5;
    float area;
    area = length * width;
    printf("area = %.2f", area);
}